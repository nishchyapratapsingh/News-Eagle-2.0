def translate_to_english(text: str, source_lang: str) -> str:
    """
    Placeholder for translation pipeline.
    """
    if source_lang == "en":
        return text
    return f"[Translated from {source_lang}]: {text}"
